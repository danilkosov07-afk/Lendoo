# Lendoo - Landing Page

Продающий сайт digital-студии, специализирующейся на лендингах, анимированных интерфейсах, data-визуале и интерактивных hero-блоках.

## 🚀 Готовность к деплою

Проект полностью готов к деплою на Vercel. См. [DEPLOY.md](./DEPLOY.md) для инструкций.

## ✨ Особенности

- ✅ **SEO оптимизация** - Meta-теги, OG-теги, Schema.org
- ✅ **Производительность** - Lazy loading, оптимизация шрифтов и изображений
- ✅ **Масштабируемость** - Структура для кейсов, мультиязычности
- ✅ **Чистый код** - Все тексты из JSON, без хардкода
- ✅ **Готовность к деплою** - Build без ошибок, production-ready

## Концепция

Сайт создан в стиле **Future / Tech / Digital** с акцентом на:
- Технологичность и футуристичность
- Экспертизу и доверие
- Минимализм с визуальными акцентами
- Чёткую структуру с атмосферными фонами

## Технологии

- **Next.js 14** (App Router)
- **TypeScript**
- **TailwindCSS**
- **Framer Motion**
- **Orbitron** (футуристичный шрифт для заголовков)
- **Inter** (нейтральный шрифт для текста)
- Без UI-библиотек

## Цветовая палитра

- **Фон**: `#050505` / `#0B0B0B`
- **Текст**: `#FFFFFF` / `#E5E5E5`
- **Акцент**: `#4FD1C5` (teal) / `#38BDF8` (blue)
- **Дополнительно**: `#1F1F1F`, `#94A3B8`

## Начало работы

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Сборка для production

```bash
npm run build
```

### Запуск production-сервера

```bash
npm start
```

### Линтинг

```bash
npm run lint
```

## Деплой на Vercel

Проект готов к деплою на Vercel:

1. Загрузите проект в GitHub/GitLab/Bitbucket
2. Импортируйте проект в [Vercel](https://vercel.com)
3. Vercel автоматически определит настройки Next.js
4. Нажмите Deploy

Или используйте Vercel CLI:

```bash
npm i -g vercel
vercel
```

## Структура проекта

```
landing-studio/
├── app/
│   ├── layout.tsx      # Корневой layout с шрифтами
│   ├── page.tsx        # Главная страница
│   └── globals.css     # Глобальные стили и переменные
├── components/         # Компоненты
│   ├── Header.tsx     # Навигация
│   ├── Hero.tsx       # Hero-блок с интерактивными элементами
│   ├── Services.tsx   # Секция услуг
│   ├── About.tsx      # О студии
│   ├── Contact.tsx    # Контакты
│   └── Footer.tsx     # Футер
├── public/            # Статические файлы
├── next.config.js     # Конфигурация Next.js
├── tailwind.config.ts # Конфигурация TailwindCSS
├── tsconfig.json      # Конфигурация TypeScript
└── package.json       # Зависимости проекта
```

## Особенности

- ✅ **Future/Tech стиль** с темной темой
- ✅ **Адаптивная типографика** с `clamp()` для всех размеров
- ✅ **Два шрифта**: Orbitron (заголовки) + Inter (текст)
- ✅ **Интерактивный Hero-блок** с реакцией на курсор
- ✅ **Плавные анимации** через Framer Motion
- ✅ **Оптимизация шрифтов** через next/font
- ✅ **Чистая архитектура** с переиспользуемыми компонентами
- ✅ **Готовность к деплою** на Vercel
- ✅ **Минимум зависимостей**
- ✅ **Без ошибок** build и lint

